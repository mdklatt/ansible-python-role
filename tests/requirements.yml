## Requirements for the tmpdir role.
##
##   ansible-galaxy install -r requirements.yml -p path/to/roles
##
## If the role itself is installed using ansible-galaxy its dependencies (see
## meta/main.yml) will be installed automatically, but for testing these
## requirments must be installed manually.
##
- src: "https://github.com/mdklatt/ansible-tmpdir-role"
  name: tmpdir
